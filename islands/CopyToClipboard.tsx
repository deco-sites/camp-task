import type { Props } from "../components/ui/CopyToClipboard.tsx";
import Component from "../components/ui/CopyToClipboard.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
